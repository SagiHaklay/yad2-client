import { LocationDetailsPipe } from './location-details-pipe';

describe('LocationDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
